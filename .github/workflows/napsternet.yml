name: NapsternetV File Forwarder

on:
  schedule:
    - cron: '*/20 * * * *'

  workflow_dispatch:

  push:
    branches:
      - main
    paths:
      - 'napsternet_forwarder.py'
      - '.github/workflows/napsternet.yml'
      - 'requirements.txt'

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  forward-napsternet-files:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Prevent workflow loop
        run: |
          if [[ "${{ github.actor }}" == "github-actions[bot]" ]]; then
            echo "Workflow triggered by bot. Exiting."
            exit 0
          fi

      - name: Random delay
        run: sleep $((RANDOM % 180 + 30))

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run NapsternetV Forwarder
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHANNEL_ID: ${{ secrets.CHANNEL_ID }}
        run: python napsternet_forwarder.py
        timeout-minutes: 25

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Stage changes
        run: |
          git add napsternet_history.json
          git add napsternet_files/ || true

      - name: Check for changes
        id: changes
        run: |
          if git diff --cached --quiet; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit changes
        if: steps.changes.outputs.changed == 'true'
        run: |
          git commit -m "📁 NapsternetV update $(date -u '+%Y-%m-%d %H:%M UTC')" || true

      - name: Pull latest changes
        if: steps.changes.outputs.changed == 'true'
        run: |
          git pull --rebase origin main || true

      - name: Push changes
        if: steps.changes.outputs.changed == 'true'
        run: |
          git push origin main
